{
    "name": "flux-app",
    "version": "0.1.0-alpha3",
    "private": true,
    "engines": {
        "node": "^12.14.1",
        "npm": "^7.0.15",
        "yarn": "^1.12.4"
    },
    "browserslist": [
        "since 2019-12"
    ],
    "lint-staged": {
        "*.{tsx, ts, js, scss}": [
            "prettier --write",
            "git add"
        ]
    },
    "dependencies": {
        "concurrently": "5.3.0"
    },
    "scripts": {
        "install": "yarn install:server && yarn install:client && yarn install:shared",
        "install:server": "cd server && cargo fetch",
        "install:client": "cd client && yarn install",
        "install:shared": "cd shared && yarn install",
        "build": "yarn build:server && yarn build:client",
        "build:server": "cd server && cargo build",
        "build:client": "cd client && yarn build",
        "start": "concurrently \"yarn start:server\" \"yarn start:client\"",
        "start:server": "cd server && cargo run",
        "start:client": "cd client && yarn start",
        "lint": "yarn lint:server && yarn lint:client",
        "lint:server": "cd server && cargo clippy",
        "lint:client": "cd client && yarn lint",
        "serve": "cd server && cargo run",
        "heroku-postbuild": "yarn build:client"
    }
}